import { RedeleDirective } from './redele.directive';

describe('RedeleDirective', () => {
  it('should create an instance', () => {
    const directive = new RedeleDirective();
    expect(directive).toBeTruthy();
  });
});
